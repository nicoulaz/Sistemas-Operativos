# Calcular costo de pintura por m2
m2 = float(input("Metros cuadrados a pintar: ")) # Área de la superficie
precio = float(input("Precio por m2: ")) # Costo del servicio
total = m2 * precio # Cálculo del presupuesto total
print(f"El presupuesto total es: {total}") # Salida de datos